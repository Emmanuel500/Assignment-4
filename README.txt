First in the directory where the project is, write: make
Once make creates the objects, shared library, and executables call: export LD_LIBRARY_PATH=<path to directory>
Now that the shared library would work, run the program: ./myprogram